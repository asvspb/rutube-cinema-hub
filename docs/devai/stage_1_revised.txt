Роль: ты — senior frontend инженер и рефакторинг‑агент.

Контекст:
- Этап 1 выполнен частично: хуки и компоненты вынесены, `isMounted` удалён, но `App.tsx` всё ещё ~629 строк.
- Требование этапа: `App.tsx` < 500 строк и полное удаление `isMounted` паттерна.

Недостатки текущей реализации (нужно устранить):
1) `App.tsx` превышает 500 строк (сейчас ~629) — цель этапа не достигнута.
2) Крупные блоки логики остаются в `App.tsx`: пагинация/фильтрация/сортировка и обработчики UI.
3) Группировка пропсов в `Navigation`/`MainContent` не доведена до конца — избыточные пропсы раздувают `App.tsx`.
4) Нет подтверждения, что тесты прошли после рефакторинга (нужно `lint`, `tsc`, `test`, `build`).

Цель этапа 1 (уточнённая):
- Сократить `App.tsx` до < 500 строк, полностью убрать `isMounted`, сохранив поведение и UI.

Обязательные исправления:
1) Полностью заменить `isMounted` на `AbortController` во всех async‑эффектах в `App.tsx` (включая загрузку home‑ленты и загрузку канала). 
   - Используй `AbortController` на уровне эффекта и передавай `signal` в асинхронные операции там, где возможно.
   - После `abort` предотвращай setState: проверяй `signal.aborted`.
2) Вынести **крупные блоки логики и состояния** из `App.tsx`:
   - `handleAnalyzeVideo`, `handleSaveMetadata`, `metadataCache` и связанный state — в отдельный хук (например `useMetadata`).
   - Логику пагинации, `ITEMS_PER_PAGE`, `currentPage`, `displayedVideos`, `handlePageChange` — в `usePagination`.
   - Логику меню каналов (menu position, refs, handlers) — в `useChannelMenu` или `useUIState` (если уже есть).
   - Логику загрузки видео (home + channel) — в отдельный хук (`useVideoLogic`), чтобы эффект и функции загрузки ушли из `App.tsx`.
3) Сократить `App.tsx` за счёт группировки props в объекты, а также передачи группированных проп‑объектов в `Navigation`/`MainContent`.
   - Допустимо создать промежуточные объекты `navigationProps`, `mainContentProps`.
4) Проверить, что `CachedPlaylistData` в `types.ts` (уже вынесено) и не дублируется.

Ограничения:
- Не менять UI/UX и бизнес‑логику.
- Не добавлять новые зависимости.
- Хуки должны иметь чёткие интерфейсы и быть переиспользуемыми.

Ожидаемый результат:
- `App.tsx` < 500 строк.
- `isMounted` полностью удалён.
- Логика декомпозирована на хуки с чёткими ответственностями.
- Поведение приложения не изменено.

Проверки:
- `npm run lint`
- `npx tsc --noEmit`
- `npm run test`
- `npm run build`

Definition of Done:
- Условия ТЗ выполнены, тесты зелёные, ошибок в браузере/сервере нет, данные загружаются, приложение работоспособно.
