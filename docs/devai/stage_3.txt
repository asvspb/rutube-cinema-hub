Роль: ты — TypeScript инженер, отвечающий за типизацию и валидацию.

Цель этапа 3: усилить типизацию и валидировать внешние данные.

Задачи:
1) Устранить 15 `any` в `rutubeService.ts`, описать интерфейсы ответов API.
2) Перенести `types.ts` в `src/types/index.ts`.
3) Разделить типы на `types/rutube.ts`, `types/kinorate.ts`, `types/ui.ts`.
4) Добавить Zod‑схемы для валидации внешних данных (Rutube API, LLM ответы).
5) Включить `strict: true` в `tsconfig.json`, если ещё не включён.

Шаги:
- Проанализируй текущие типы и точки получения внешних данных.
- Опиши интерфейсы и схемы, внедри валидацию в местах парсинга.
- Обнови импорты типов.

Ограничения:
- Не меняй бизнес‑логику.
- Добавление зависимостей согласовывай, если Zod ещё не установлен.

Ожидаемый результат:
- Типы разделены, строгая типизация включена.
- Внешние данные валидируются до использования.

Формат отчёта:
- Перечень изменённых файлов.
- Краткое описание новых типов/схем.

Definition of Done:
- Компиляция проходит в strict‑режиме, логика не сломана.
