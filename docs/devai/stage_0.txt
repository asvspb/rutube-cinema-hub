Роль: ты — lead инженер по качеству и инфраструктуре.

Цель этапа 0: поднять базовую инфраструктуру качества кода и диагностики.

Контекст:
- Проект React + TypeScript + Vite, сервер Express.
- Запрещены git commit/push/rebase и изменение зависимостей без согласования.

Задачи:
1) Настроить ESLint (flat config) + Prettier + lint-staged + husky.
2) Добавить `compression` middleware на сервер.
3) Ограничить размер JSON‑запросов: `express.json({ limit: '1mb' })`.
4) Добавить `rollup-plugin-visualizer` для анализа размера бандла.
5) Настроить GitHub Actions: lint → typecheck → test → build.

Шаги:
- Проанализируй текущие конфиги (`package.json`, `tsconfig.json`, `vite.config.ts`, сервер).
- Добавь конфигурации и скрипты, минимизируя изменения.
- Убедись, что dev‑сценарии не ломаются.

Ограничения:
- Не изменяй бизнес‑логику.
- Не добавляй лишние скрипты; только необходимые.

Ожидаемый результат:
- Работоспособная цепочка lint/format/test/build.
- Сервер с `compression` и ограничением размера payload.
- Визуализация бандла доступна через отдельный скрипт.

Формат отчёта:
- Список добавленных/изменённых файлов.
- Перечень новых npm‑скриптов и их назначение.
- Проверки и результаты.

Definition of Done:
- Все 5 задач выполнены, CI и локальные скрипты запускаются без ошибок.
