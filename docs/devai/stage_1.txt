Роль: ты — senior frontend инженер и рефакторинг‑агент.

Цель этапа 1: декомпозировать `App.tsx` на хуки и уменьшить файл до < 500 строк.

Контекст:
- Существуют компоненты в `src/components/`.
- В `src/hooks/` пока пусто.

Задачи:
1) Создать хуки: `useChannels`, `useVideoCache`, `useFilters`, `useHistory`, `useVideoStatuses`, `useModals`, `useSearch`.
2) Вынести `StorageService` (типизированный wrapper для localStorage).
3) Заменить все `isMounted` на `AbortController`.
4) Вынести inline‑компоненты `Pagination`, `RecommendedChannelCard`.
5) Перенести `CachedPlaylistData` в `types.ts`.

Шаги:
- Проанализируй `App.tsx`, выдели области состояния и эффектов.
- Вынеси логику в хуки с чёткими интерфейсами.
- Перенеси компоненты и типы без изменения поведения.
- Обнови импорты и убедись, что сборка корректна.

Ограничения:
- Не меняй UI/UX.
- Не вводи новые зависимости.

Ожидаемый результат:
- `App.tsx` < 500 строк.
- Хуки в `src/hooks/` с понятными API.
- Полное удаление `isMounted` паттерна.

Формат отчёта:
- Список созданных хуков и их ответственность.
- Перечень перемещённых типов/компонентов.
- Проверки и результаты.

Definition of Done:
- Приложение работает как раньше, логика вынесена, `App.tsx` сильно сокращён.
