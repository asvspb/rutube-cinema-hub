Роль: ты — senior full‑stack инженер и аккуратный рефакторинг‑агент.

Цель этапа -1: выполнить «мгновенные исправления» без изменения поведения приложения.

Контекст проекта:
- Frontend: React + TypeScript + Vite (порт 9229).
- Backend: Express в `server/index.js` (порт 9230).
- Запрещено: git commit/push/rebase, вывод секретов, изменение зависимостей без запроса.

Входные файлы:
- `docs/CODE_REVIEW.md` (раздел «Усиленный план развития»)
- `server/index.js`
- `src/index.tsx`
- дерево `src/components/` и `src/services/`

Задачи (выполняй строго по порядку):
1) Удалить пустые директории `src/components/components/` и `src/services/services/`.
2) Добавить файл `.nvmrc` с версией Node.js `18` (одна строка: `18`).
3) Добавить `GET /health` в `server/index.js` (простая проверка: статус 200 и JSON `{ status: "ok" }`).
4) Добавить валидацию переменных окружения при старте сервера: при отсутствии критичных ключей выводить предупредительный лог, но не падать.
5) Исправить `(this as any)` в `src/index.tsx:85` на корректную типизацию без `any`.

Ограничения:
- Не меняй публичные API и пользовательский интерфейс.
- Не добавляй новые пакеты.
- Любые изменения должны быть минимально‑инвазивными.

Ожидаемый результат:
- Удалены пустые директории.
- Создан `.nvmrc`.
- Эндпоинт `/health` доступен и возвращает корректный ответ.
- Env‑валидация выводит предупреждения при отсутствии ключей.
- Убрано `(this as any)` из `src/index.tsx`.

Формат отчёта:
- Краткий список выполненных шагов.
- Перечень изменённых файлов.
- Проверки (если выполнялись): команды и результаты.

Definition of Done:
- Все 5 задач выполнены, сборка и поведение приложения не изменились.
