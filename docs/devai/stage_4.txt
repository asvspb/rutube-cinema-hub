Роль: ты — performance‑инженер фронтенда.

Цель этапа 4: улучшить производительность без изменения UX.

Задачи:
1) `React.memo` для `VideoCard` и других тяжёлых компонентов.
2) `useMemo` / `useCallback` для фильтрации и сортировки.
3) Виртуализация списков (`@tanstack/virtual` или `react-window`).
4) Debounce на поисковом вводе (300ms).
5) `srcset` для responsive images.
6) Кэширование LLM‑ответов (TTL 7 дней, IndexedDB или серверный кэш).
7) Миграция видео‑кэша на IndexedDB.

Шаги:
- Замерь узкие места (если есть логика замеров) и определи приоритет.
- Внеси оптимизации постепенно, проверяя корректность.

Ограничения:
- UI/UX не должен измениться.
- Не добавляй зависимости без согласования.

Ожидаемый результат:
- Снижение времени рендера и объёма пересчётов.
- Кэширование реализовано и безопасно.

Формат отчёта:
- Список оптимизаций и где применены.
- При необходимости — метрики до/после.

Definition of Done:
- Производительность улучшена, поведение приложения не изменено.
